version: '2'
services:
  app:
    build: ./app
    ports:
     - "3000:3000"
    volumes:
     - ./app/src:/usr/src/app/src
  db:
    image: mongo
    ports:
     - "27017:27017"
  backend:
    build: ./backend
    ports:
      - "4000:4000"
